{
  "$schema": "https://turborepo.org/schema.json",
  "pipeline": {
    "prepare": {},
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["build/**", "dist/**"]
    },
    "build:mock": {
      "dependsOn": ["^build"],
      "outputs": ["build/**", "dist/**"]
    },
    "lint": {
      "outputs": []
    },
    "start": {
      "cache": false
    },
    "mock": {
      "cache": false
    }
  }
}
