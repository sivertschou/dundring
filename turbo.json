{
  "$schema": "https://turborepo.org/schema.json",
  "pipeline": {
    "prepare": {},
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["build/**", "dist/**"]
    },
    "build:mock": {
      "dependsOn": ["^build"],
      "outputs": ["build/**", "dist/**"]
    },
    "lint": {
      "outputs": []
    },
    "start": {
      "dependsOn": ["^build"],
      "cache": false
    },
    "mock": {
      "cache": false
    }
  }
}
